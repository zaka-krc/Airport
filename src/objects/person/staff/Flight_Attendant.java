package objects.person.staff;
import objects.person.Person;

public class Flight_Attendant extends Person {
    public Flight_Attendant(String name, int age, String adress) {
        super(name, age, adress);
    }
}
